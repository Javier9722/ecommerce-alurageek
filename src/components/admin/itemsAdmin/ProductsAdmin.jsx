import React from "react";

export const ProductsAdmin = ({ id, productos }) => {
  console.log(id);
  console.log(productos);
  return (
    <div>
      <h1>Productos del admin xd</h1>
    </div>
  );
};
